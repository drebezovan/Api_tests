url: "jdbc:postgresql://localhost:5432/MTS_Credit_App_TESTS"
username: "user"
password: "1234"
junit:
  jupiter:
    execution:
      parallel:
        enabled: true
        config:
          strategy: dynamic